# BMad MAO — 설계 히스토리

> 이 문서는 BMad MAO가 어떤 대화와 고민에서 탄생했는지를 기록합니다.
> 코드보다 **왜(WHY)** 이렇게 만들었는지를 남기는 문서입니다.

---

## 시작점 — "에이전트를 동시에 돌리면 어떨까?"

BMad Method를 사용하면서 자연스러운 질문이 생겼습니다.

> *"에이전트 하나가 전체를 처리하는 게 아니라, 여러 에이전트를 동시에 돌리고 결과를 합칠 수 있지 않을까?"*

Claude Code의 **Background Agent** 기능(드리밍 에이전트)과 **Worktree 격리** 기능이 이를 가능하게 한다는 걸 확인하면서 설계가 시작됐습니다.

**핵심 전제:**
- 각 에이전트는 독립된 git worktree에서 실행 → 충돌 없음
- 오케스트레이터가 결과물을 수집·검증 → PRD 기준으로 통합

---

## 첫 번째 고민 — "에이전트들이 서로 연결이 안 되면?"

병렬로 실행된 에이전트들의 결과물이 **서로 연결되지 않는 문제**가 예상됐습니다.

예: 프론트엔드 에이전트는 `user_id`를 string으로 가정, 백엔드 에이전트는 int로 구현 → 통합 시 충돌.

**해결책: 계약서(Contract) 개념 도입**

PRD와 계약서를 분리했습니다.

| | PRD | 계약서 |
|--|-----|--------|
| 담는 것 | WHY / WHAT | HOW TO CONNECT |
| 내용 | 제품 목표, 사용자 요구사항 | API 명세, 데이터 타입, 파일 범위, 금지 사항 |
| 작성 시점 | 개발 착수 전 | 에이전트 배포 직전 |
| 변경 빈도 | 낮음 | 중간 |

계약서는 에이전트 브리핑에 직접 포함되어 각 에이전트가 자신의 범위와 인터페이스를 명확히 알게 됩니다.

---

## 두 번째 고민 — "얼마나 자유를 줄 것인가?"

계약서를 엄격하게 지키면 에이전트 자율성이 줄어들고, 너무 느슨하면 계약 의미가 없습니다.

**해결책: 실행 모드 3종**

| 모드 | 설명 | 에이전트 자율도 |
|------|------|----------------|
| **타이트** | 계약서 100% 준수, 이탈 금지 | 낮음 |
| **러프** | 계약서를 가이드라인으로만 활용 | 높음 |
| **혼합** | 핵심 인터페이스만 타이트, 나머지 자율 | 중간 (기본값) |

혼합 모드가 기본값인 이유: 인터페이스(연결부)는 타이트하게 잡되, 구현 방식은 에이전트가 더 잘 판단할 수 있다는 원칙에서.

또한 `/mao-create-contracts` 단계에서 **다른 에이전트들의 의견을 들어보는 딥다이브** 옵션("A 깊게 봐줘")을 추가해 사용자의 이해도도 높이도록 했습니다.

---

## 세 번째 고민 — "PM 에이전트가 결과물을 다 읽으면 컨텍스트가 터진다"

오케스트레이터가 모든 에이전트의 결과물을 한 번에 읽으면 컨텍스트 한계를 초과합니다.

**해결책: 3단계 점수화 시스템**

결과물을 한 번에 다 읽는 대신, 점수로 먼저 요약하고 필요한 것만 자세히 봅니다.

```
L1 — 자동 점수 (가중 평균, 10점 만점)
  ├── 계약서 준수도   40%
  ├── PRD 커버리지    30%
  ├── 통합 호환성     20%
  └── 코드 품질       10%

L2 — 요약 검토 (점수 6~7점대)
  → README, index.ts 등 요약 파일만 읽음

L3 — 상세 검토 (점수 5점 이하)
  → 문제 파일만 타겟 읽기
```

**통과 기준:**
- 8점 이상 → 통과
- 6~7점 → L2 요약 검토 후 재판단
- 5점 이하 → L3 상세 검토 + 재브리핑

---

## 네 번째 고민 — "계속 실패하는 에이전트는 어떻게 개선하나?"

같은 에이전트가 반복해서 낮은 점수를 받으면, 재브리핑만으로는 한계가 있습니다.

**해결책: 자동 업그레이드 루프**

3회 연속 실패 또는 2회 연속 점수 하락이 감지되면:
1. 실패 패턴 분석
2. 자동 리서치 (유사 사례, 더 나은 접근법 탐색)
3. 개선 제안 (에이전트 프롬프트 또는 계약서 수정안)
4. **사용자 승인 후** 적용

"에이전트가 자동으로 자기 자신을 고친다"는 개념으로, BMad 정신("사람이 결정, 에이전트가 실행")을 지키기 위해 최종 적용은 항상 사용자 승인을 거칩니다.

---

## 다섯 번째 고민 — "계약서 생성 전에 꼭 질문이 많아야 하나?"

초기 설계에서 `/mao-create-contracts`는 PRD에서 계약서로 넘어갈 때 많은 질문을 했습니다.

> *"굳이 그럴 필요가 있을까?"*

검토 결과, 질문이 꼭 필요한 경우는 **실행 모드 선택**과 **딥다이브 여부** 두 가지뿐이었습니다. 나머지는 PRD에서 자동으로 추론 가능. 질문을 최소화하는 방향으로 단순화했습니다.

---

## 여섯 번째 고민 — "BMad가 너무 무겁다. 빠른 탐색이 필요하다"

BMad를 쓰다 보면 팀과 가볍게 아이디어를 맞춰보고 싶을 때도 PRD → 계약서 → 배포의 전체 흐름을 타야 해서 불편했습니다.

> *"가볍게 단계를 축소해서 빠른 아웃풋이 나와서 팀원들과 협의할 수 있으면 좋겠어. 완성도가 목표가 아니야."*

**해결책: mao-spark (v0.2.0 신규)**

HTML 프로토타입을 **질문 없이 즉시** 만들어 팀 투표를 가능하게 하는 스킬.

설계 원칙:
- 첫 반복: 3가지 방향(A/B/C)으로 팀 투표
- 이후 반복: 선택된 방향으로 단일 파일 개선
- 5회 후 졸업 제안 → `/mao-create-contracts`로 자연스럽게 연결
- 피드백의 이유를 선택적으로 기록 → 여정 요약에 반영

**BMad 정신 유지 포인트:**  
자동으로 모드를 감지하되, **한 줄 확인 후 진행**합니다. "사람이 결정한다"는 원칙은 Spark에서도 동일합니다.

---

## 핵심 설계 원칙 (최종)

1. **PRD와 계약서는 분리한다** — PRD는 WHY/WHAT, 계약서는 HOW TO CONNECT
2. **사람이 결정, 에이전트가 실행** — 모든 중요한 선택은 사용자 확인을 거친다
3. **컨텍스트는 계층적으로 관리한다** — 전체를 한 번에 읽지 않고 필요한 층위만
4. **점수로 소통한다** — 10점 만점 기준으로 현재 상태를 수치화
5. **실패는 학습 기회다** — 반복 실패 패턴을 자동 감지해 시스템 자체를 개선
6. **탐색과 실행을 분리한다** — Spark(탐색) → MAO(실행)의 자연스러운 흐름

---

## 버전 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v0.1.0 | 2026-05-27 | 최초 설계 및 릴리스. 스킬 4종: mao-create-contracts, mao-orchestrate, mao-score-review, mao-auto-upgrade |
| v0.2.0 | 2026-05-27 | mao-spark 추가. "빠른 탐색" 레이어 도입으로 전체 흐름 완성 |
